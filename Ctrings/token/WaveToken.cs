using Microsoft.Xna.Framework;

namespace Ctrings.token {
	public class WaveToken : Token {
		public override Vector2 Render(CFont font, Vector2 start, Ctring ctring) {
			for (var i = 0; i < Text.Length; i++) {
				
			}

			return Vector2.One;
		}
	}
}